{
  "name": "restpad",
  "displayName": "Restpad - REST API Client",
  "description": "Fast, offline REST API client for .http files. Free, open source, and REST Client compatible. Environments, variables, request chaining, cookies, curl import. No account, no cloud.",
  "version": "0.0.1",
  "publisher": "KeeganJones",
  "license": "MIT",
  "private": true,
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Testing",
    "Programming Languages"
  ],
  "keywords": [
    "rest client",
    "restclient",
    "http client",
    "httpclient",
    "api client",
    "apiclient",
    "rest",
    "http",
    "api",
    "rest api",
    "postman",
    "insomnia",
    "thunder client",
    "bruno",
    "httpyac",
    "rest-client",
    "curl",
    "http request",
    "api testing",
    "dotenv",
    "environments"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/jeegankones/restpad"
  },
  "icon": "icon.png",
  "main": "./dist/extension.js",
  "activationEvents": [
    "onLanguage:http"
  ],
  "contributes": {
    "languages": [
      {
        "id": "http",
        "aliases": [
          "HTTP",
          "http"
        ],
        "extensions": [
          ".http",
          ".rest"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "http",
        "scopeName": "source.http",
        "path": "./syntaxes/http.tmLanguage.json"
      }
    ],
    "commands": [
      {
        "command": "restpad.sendRequest",
        "title": "Restpad: Send Request"
      },
      {
        "command": "restpad.sendAllRequests",
        "title": "Restpad: Send All Requests"
      },
      {
        "command": "restpad.cancelRequest",
        "title": "Restpad: Cancel Request"
      },
      {
        "command": "restpad.switchEnvironment",
        "title": "Restpad: Switch Environment"
      },
      {
        "command": "restpad.showHistory",
        "title": "Restpad: Show Request History"
      },
      {
        "command": "restpad.saveResponse",
        "title": "Restpad: Save Last Response Body"
      }
    ],
    "configuration": {
      "title": "Restpad",
      "properties": {
        "restpad.timeout": {
          "type": "number",
          "default": 30000,
          "description": "Request timeout in milliseconds."
        },
        "restpad.followRedirects": {
          "type": "boolean",
          "default": true,
          "description": "Automatically follow HTTP redirects."
        },
        "restpad.environmentVariables": {
          "type": "object",
          "default": {},
          "description": "Environment variables, keyed by environment name. Use \"$shared\" for variables available in every environment. Compatible with rest-client.environmentVariables."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "npm run build && vsce package --no-dependencies",
    "compile:integration": "tsc -p tsconfig.integration.json",
    "pretest:integration": "npm run build && npm run compile:integration",
    "test:integration": "node dist-test/integration/runTest.js"
  },
  "dependencies": {
    "undici": "^7.10.0"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.15.0",
    "@types/vscode": "^1.90.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.25.5",
    "glob": "^13.0.6",
    "mocha": "^11.7.6",
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
